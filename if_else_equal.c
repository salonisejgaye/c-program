// wap to check two values are equal or not
#include<stdio.h>
void main()
{
    int a,b;
    printf("enter two values");
    scanf("%d%d",&a,&b);
    if(a==b)
    {
        printf("both values are equal");
    }
    else
    {
        printf("both values are different");
    }
}